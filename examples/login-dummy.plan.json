{
  "version": "1",
  "name": "Dummy login example with secrets",
  "steps": [
    { "type": "goto", "url": "https://example.test/login" },
    { "type": "login", "username": "${SECRET:USERNAME}", "password": "${SECRET:PASSWORD}" },
    { "type": "expect", "urlIncludes": "dashboard" }
  ]
}
